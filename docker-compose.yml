version: '3.8'
services:
    frontend:
        image: lst97/simple-cms-frontend:dev
        ports:
            - '1167:80'
        expose:
            - '80'
        volumes:
            - ~/app:/usr/src/app:ro

volumes:
    mongodb_data:
